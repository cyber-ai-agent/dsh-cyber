name: integrate-pr8

on:
  push:
    branches: [merge/pr-8-integration]

permissions:
  contents: write

jobs:
  merge-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: merge/pr-8-integration
      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Fetch PR 8 head
        run: git fetch origin refs/pull/8/head:refs/remotes/origin/pr8
      - name: Attempt merge
        id: merge
        shell: bash
        run: |
          rm -f PR8_CONFLICTS.txt PR8_CONFLICT_PATCH.diff PR8_SERVER_CONFLICT.txt PR8_APP_CONFLICT.txt PR8_CHAT_CONFLICT.txt
          set +e
          git merge --no-ff --no-edit refs/remotes/origin/pr8
          status=$?
          if [ "$status" -ne 0 ]; then
            git diff --name-only --diff-filter=U > /tmp/pr8-conflicts.txt
            git diff --cc > /tmp/pr8-conflict.patch
            cp packages/server/src/server.ts /tmp/pr8-server.txt
            cp packages/web/src/App.tsx /tmp/pr8-app.txt
            cp packages/web/src/components/ChatWorkbench.tsx /tmp/pr8-chat.txt
            git merge --abort || true
            cp /tmp/pr8-conflicts.txt PR8_CONFLICTS.txt
            cp /tmp/pr8-conflict.patch PR8_CONFLICT_PATCH.diff
            cp /tmp/pr8-server.txt PR8_SERVER_CONFLICT.txt
            cp /tmp/pr8-app.txt PR8_APP_CONFLICT.txt
            cp /tmp/pr8-chat.txt PR8_CHAT_CONFLICT.txt
            echo "conflicted=true" >> "$GITHUB_OUTPUT"
          else
            echo "conflicted=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Persist conflict diagnostics
        if: steps.merge.outputs.conflicted == 'true'
        run: |
          git add -f PR8_CONFLICTS.txt PR8_CONFLICT_PATCH.diff PR8_SERVER_CONFLICT.txt PR8_APP_CONFLICT.txt PR8_CHAT_CONFLICT.txt
          git commit -m "ci: capture exact PR 8 conflict state"
          git push origin HEAD:merge/pr-8-integration
      - name: Stop on conflicts
        if: steps.merge.outputs.conflicted == 'true'
        run: exit 1
      - uses: pnpm/action-setup@v4
        if: steps.merge.outputs.conflicted == 'false'
        with:
          version: 11.7.0
          run_install: false
      - uses: actions/setup-node@v4
        if: steps.merge.outputs.conflicted == 'false'
        with:
          node-version: 22.19.0
          cache: pnpm
      - if: steps.merge.outputs.conflicted == 'false'
        run: pnpm install --frozen-lockfile
      - if: steps.merge.outputs.conflicted == 'false'
        run: pnpm typecheck
      - if: steps.merge.outputs.conflicted == 'false'
        run: pnpm test
      - if: steps.merge.outputs.conflicted == 'false'
        run: pnpm exec playwright install --with-deps chromium
      - if: steps.merge.outputs.conflicted == 'false'
        run: pnpm test:e2e
      - name: Push verified merge commit
        if: steps.merge.outputs.conflicted == 'false'
        run: git push origin HEAD:merge/pr-8-integration
