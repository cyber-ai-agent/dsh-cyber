# 2026-09-05 审计整改执行计划

审计来源：`dsh-cyber-audit-2026-09-05.md` v1.0。审计与开发起点均为 `3e922bfee3915c1072223654e52c026766ef2c0c`。执行分支：`codex/audit-remediation-20260905`。

## 执行方式

先验证发现仍成立，再以故障注入测试固定行为，最后实现修复。E1 独立语义复现必须转为仓库回归测试；E2 核对完整调用链；E3 保留为待验证风险，不能写成线上事故。

按以下顺序形成独立提交及审查单元。当前批次验证通过后继续下一批；公共契约变更由主 agent 统一整合，Luna max 子 agent 负责不冲突的实现、测试或审阅。每批记录测试结果、限制、提交号；未通过门禁不得标记完成。开发过程只使用隔离临时数据进行故障测试。

如拆分后续 PR，每个新开发分支重新 fetch 并从当时 `origin/main` 创建；依赖尚未合并时先处理依赖，不从旧开发分支宣称最新基线。本轮一个整改分支内按顺序提交，不重复创建相互依赖的平行实现。

## 九批交付

| 批次 | 范围 | 实现边界 | 必须提供的证据 | 状态 |
| --- | --- | --- | --- | --- |
| 01 | F01–F03 | 重试循环、运行映射、会话级 reset、适配器代际租约和退役 | 重试中审批/停止、双会话隔离、并发配置变更、工厂失败、关闭竞争 | 已提交，CI 通过 |
| 02 | F04/F05 | 稳定读取、临时发布及读回、恢复 journal、失败回滚与独占保护 | 同尺寸改写、增长/删除、逐步 rename 故障、进程中断后恢复、救援副本保留 | 已提交，CI 通过 |
| 03 | F07/F13/F17 部分 | 按世界和会话持有草稿，上传回调归属、逐文件状态；先采用诚实的“清空草稿”语义 | 上传中切会话/世界、部分失败、删除/卸载、可见入口浏览器冒烟 | 已提交，含浏览器的 CI 通过 |
| 04 | F08 | 在现有 WorkTurn/队列上统一受理：聊天 → 任务 → 日程 | 同 key 幂等/冲突、授权一致、审批重启、无双重派发 | 已提交，CI 通过 |
| 05 | F09 | 依赖 04；日程受理解耦执行、有界并发、公平性、关闭期限 | 长任务不阻塞独立任务、停止期限、错过触发和时区、未知结果不重做 | 已提交，CI 通过 |
| 06 | F06/F10/F11 | 最终有效上下文、硬预算、检查器来源、稳定 FTS 候选 | 捕获实际入参比对、超限不请求、私聊边界、50/500/5000 候选评测 | 开发中 |
| 07 | F12/F14/F16 | SSE 缓冲上限、订阅异常隔离、状态所有权、保存反馈、布局退出/键盘 | 慢连接、异常订阅者、保存失败、pointercancel、Profiler 和滚动锚点 | 待执行 |
| 08 | F15 | 服务端路由解释与能力事实，未知能力显式呈现 | 继承矩阵、临时覆盖、删除回退、参数拒绝、缓存事实区分 | 待执行 |
| 09 | F17 与产品闭环 | 首次委托、持续合作、周期工作；交付验收、真实状态投影 | 必过核心浏览器冒烟；三视口、小屏、200% 缩放、键盘；3D/语音资源生命周期 | 待执行 |

F06 的硬固定层超限属于高优先级：在 01–03 后重新评估可达性，若测试确认会损坏正常请求，可提前交付独立拒绝保护；完整上下文整合仍属于 06，避免与统一受理层同时改装配。

只读复核已确认普通聊天最多可输入 32,000 个 Unicode 字符，而小窗口模型的固定文本统计会被压低；规划后仍会追加 Persona/世界/Skill/产物提示。安排在 03 后先交付 F06 固定输入拒绝保护（不静默丢弃当前请求）。该保护只约束明确计入的文本，不能冒充已经覆盖 Harness 保留历史与全部原生工具 schema；完整有效上下文与检查器一致性仍在 06 验收。

审计中的架构/交互建议不是自动生效的产品指令。第 14 节建议的左侧角色导航与仓库护栏冲突，因此保留左侧“仅会话”、顶栏统一市场/创意工坊、角色在档案管理的现有规则；不借整改恢复旧入口。性能数字仅作为待测目标：记录机器、数据量和方法后评估受理 p95 300ms、管理 API 200ms、输入回显 50ms、缓存会话切换 200ms，不提前宣称达标。

## 32 个功能面的验收登记

审计第 10 节的 32 项作为回归清单，而非 32 个新增功能需求。执行到所属批次时补充真实运行证据，尚未复测不宣称通过。

- 世界、角色与模型：启动配置；世界创建设置；归档恢复删除；角色版本授权；履历记忆成长；头像 VRM；模型继承。
- 对话与执行：私聊会话；排队补充插队；停止重试恢复；群聊；任务分工；审批；轨迹与上下文。
- 知识与交付：角色记忆检索；知识导入检索；图谱整理；任务中心；日程；产物中心。
- 技能与创作（21–28）：原生工具/宿主技能；MCP/Browser/Firecrawl；通讯 IM；市场安装升级回滚；角色生成器；世界生成器；皮肤生成器；插件生成器。IM 完整收发只作独立能力评估，本轮不扩大平台接入。
- 空间与系统（29–32）：2D/3D 空间；语音输入朗读；备份恢复更新诊断；多语言响应式可访问性。保留接线审阅/完整验证的差别，不以构建成功代替功能验收。

## 验证与交付标准

1. 每批运行受影响测试、类型检查与构建；跨契约修改追加相关服务集成测试。
2. 界面修改使用真实可见控件，记录 1440×900、1920×1080、3840×2160 的图片适配、空白、对比度、中文一致性、最小字号、可读性以及 console error/warn。
3. 真实 Harness canary 与无密钥确定性测试分别报告；没有真实执行证据不宣称供应商链路已通过。
4. 提交前检查差异范围及运行健康；不提交凭据、数据库、日志、生成产物。提交后 push，按精确提交检查 CI；合并与发布只采用实际通过的证据。
5. 备份/恢复测试不操作用户 stateRoot；更新不能删除本地世界、角色、资产或历史。

## 执行记录

- 2026-09-05：完成审计重点与九批依赖核对。fetch 后 HEAD、origin/main、merge-base 一致；工作树干净。开始 01，确认现有重试直接返回 Promise、角色级恢复和代际 await 竞争均存在。
- 批次 01：类型检查、生产构建及构建预算通过；全仓库 320 个测试文件通过，1760 项通过、1 项跳过（该轮之后补充的路由竞争测试单独 9/9 通过）。真实 Harness Worker 使用回环模型服务的权限、审批、会话恢复集成测试通过；未宣称外部供应商 canary 通过。隔离临时 stateRoot 的 `/api/health` 返回 ok，SQLite integrity 为 ok。无界面改动，不标记视觉验收。
- 批次 01 提交 `c10b32f` 已推送；草稿 PR #178；GitHub CI 运行 `33974316017` 的 required 成功，含干净依赖安装、类型检查、单元/集成与迁移/schema 门禁。继续 02，不提前合并未完成的整改草稿。
- 批次 02：备份/CLI/服务/独占锁定向回归通过，含 9 MiB 分片、强制恢复、进程终止后重新持锁、在线恢复拒绝、旧 schema、损坏包、回退失败及已提交事务清理；类型与构建预算通过。全仓库运行 1774 通过、1 跳过，另有装配模块类型导入循环与 Windows 临时目录 ENOTEMPTY 两项失败；循环已修正，两文件随后 13/13 定向复验通过，目录清理问题未修改产品代码，不把这次本地全量运行记为全绿，继续以远端完整 CI 复核。
- 恢复机制的验证边界为进程中断与明确 I/O 失败。临时文件和 journal 已刷盘，但未验证突然断电/磁盘硬件损坏下的所有文件系统保证。正式备份发布使用同文件系统硬链接，文件系统不支持时明确失败，不回退到覆盖旧备份。
- 批次 02 提交 `e2a1594e8574440cad8da9488f1bb88936701e90` 的远端 CI `33976037819` 已完整成功，`CI / required` 通过。
- 批次 03 的旧产物浏览器回归已失败复现：A 发起上传后切到 B，`owner-a.txt` 错误出现在 B 的草稿中。采用仓库 Playwright：当前没有 Browser plugin/browser skill（Browser plugin not available），不依赖外部模型；沿用现有深色/琥珀色设计 token 和现行产品信息架构。
- 2026-09-06 批次 03：104 个前端测试文件、541 项通过；随后 owner alias 修正的 3 文件 25 项定向复验通过。最终生产构建/预算通过，浏览器 Smoke 3/3 通过（13.6 秒），包含两个会话同时上传、世界切换、刷新、部分失败、清空及旧命令兼容。`/换个话题` 不再承诺重置上下文。浏览器 Smoke 已加入 required CI，失败保存截图/trace/console。

### 批次 03 视觉检查

| 视口 | 图片适配/空白 | 草稿文字对比度 | 语言/最小字号 | 文案与操作 |
| --- | --- | --- | --- | --- |
| 1440×900 | 背景与画布容器铺满，无新增黑边或横向溢出 | 附件文字 ≥ 4.5:1 | 中文状态，文件名原样；附件 ≥ 12px | 草稿可读；移除目标 40×40px |
| 1920×1080 | 容器铺满，无横向溢出 | 附件文字 ≥ 4.5:1 | 附件 ≥ 12px | 草稿可读；移除目标 40×40px |
| 3840×2160 | 容器铺满，按原比例呈现 | 附件文字 ≥ 4.5:1 | 附件 ≥ 12px | 草稿可读；移除目标 40×40px |

正常路径 console error/warn 为空；部分失败用例主动注入 HTTP 500，允许对应资源错误，pageerror 为空。截图已实际查看。世界默认 cover 取景仍可能让角色离开可见区域，本批不宣称整个世界/3D/语音及小屏视觉验收完成，完整取景和产品黄金路径仍在 09 处理。

### F06 前置固定输入保护

批次 03 提交 `a2afdc4414c8fbe13f02502ee871549bfb91d612` 的 CI `33978890860` 成功，含新核心浏览器门禁。

F06 前置保护已完成：规划不再压低固定 token 占用；最终角色系统文本和本次格式化历史/请求在创建运行时及绑定会话 ID 前再次估算。角色/权限/目录变化按新会话预检；历史 ID 冲突的完整重放再次检查，拒绝后不保留失效 ID。即时 HTTP 413 与排队失败均提供中文提示，统计不包含 prompt。

验证：整仓 323 文件、1804 项通过、1 跳过；之后补充 HTTP 无统计数据/终态去重的 21 项定向复验通过；类型与生产构建预算通过。估算不是供应商精确 tokenizer，不覆盖 Harness 已保留历史及原生工具 schema，因此 F06/F10 的完整有效上下文工作仍保留在 06，未提前标记全部完成。

前置保护提交 `efc369d35704c04edc76b40ede777ca4feaed0b9` 的 CI `33980551572` 完整通过，包含核心浏览器冒烟。

### 第四批当前检查点与接续

群聊动作上限已按现行产品规则收紧：首位实际匹配并可提出动作的执行角色返回后，立即停止其他角色的准备；每次仍以 `maxActions: 1` 在持久化/执行之前截断。任务步骤承担者优先于仅做总结的协调角色，审批继续复用既有动作。两位成员均具有同一 Skill 的回归及 Skill Runtime 共 18 项通过。

统一提交入口本身尚未完成。下一步先实现聊天受理的原子幂等与相同 key 不同请求冲突，再接任务中心及日程。不得通过只增加外层查找、只处理排队请求或重复创建会话来宣称幂等完成；委托协作等现有路径也需要核对，不能丢弃旧能力。

2026-09-06 新增 schema 44 `conversation_submission_claims` 与 `ConversationIngressService` 基础：一次事务创建/复用会话、WorkTurn、owner 消息和可选队列行；claim 只保存 fingerprint 与事实 ID，不复制回复正文。旧 `clientTurnId` 没有可信 fingerprint 时 fail closed；同成员 group 无 sessionId 继续创建独立群聊。持久化 15 文件、93 项通过；原子 claim 的跨连接回放、冲突、整体回滚、旧 key 与群聊隔离测试通过。该检查点尚未接入 HTTP 聊天路由，因此第四批仍为开发中。

Direct HTTP 接线检查点：`/chat` 的 direct immediate/queued 在 task classifier、Skill 和 runtime 之前进入同一 keyed ingress；claim 后只继续已有 WorkTurn。相同请求并发或完成后重发均返回原事实，修改 prompt 复用 key 返回 409。进程中断后若 immediate claim 仍为 queued 且没有 queue 行，可安全继续原 WorkTurn。服务器定向 16 项与全类型检查通过。Group、委托协作和旧 `/chat-queue` 入口尚未接入，第四批仍未完成。

Group HTTP 接线检查点：`/chat` 的 discussion/task immediate/queued 在 planner 前先查 claim；无命中才规划实际 reservation，并原子写入计划 metadata 与事实。无 queue 的 group 复用 `runAcceptedGroup(existingWorkTurnId)`，审批回调同样继续该回合；有 queue 仍由原 dispatcher 执行。并发 discussion、完成后 task queue 重试均保持一个 WorkTurn、owner 消息和既有 AgentRuns；不同 roster/payload 的同 key 返回 409。群聊/Skill/路由及架构定向 21 项通过。委托协作与旧 `/chat-queue` 仍待接入。

旧 `/chat-queue` direct 兼容入口已改为先 claim 后建会话；与 `/chat` 的 `queueMode=normal` 可跨入口回放同一 queue/WorkTurn。无 sessionId 重试不再创建空会话，改变 payload 返回 409；无 clientTurnId 的旧调用保持原行为但不承诺幂等。构建预算通过，受理/队列/持久化定向 23 项通过。委托协作、任务中心与日程仍未接入。

已创建当前任务的“DSH Cyber 审计持续整改”自动接续（每小时），沿用此分支与 PR #178。电脑和桌面应用运行时继续未完成批次；每批更新证据，完成全部整改后暂停。自动接续不是剩余批次已完成的声明。

第四批受理边界继续扩展：委派协作先 claim 原始 direct WorkTurn，再执行 peer 协作，并以 `continueDirect(existingWorkTurnId)` 汇报；同进程重复等待首个执行，进程重启后 running/interrupted/终态只回放，不重新产生 peer 副作用。委派排队当前明确返回 422，避免绕开幂等边界。Task Center 由服务端计算请求 fingerprint，在模型执行前一次事务创建 TaskRun attempt、任务会话、WorkTurn、owner 消息、计划与分配；失败 attempt 保留，重试创建新 attempt。日程以 `(scheduleId, scheduledFor)` 原子创建 run、canonical direct 会话、WorkTurn 与消息，并让模型继续该 WorkTurn；重启中断未知结果，不重做同一时间片。schema 已推进到 46。

提交 `b79be` 的 CI 暴露 accepted group 在 `queued` 状态直接进入审批，以及角色名“档案管理员”被误判为管理命令。修复后先启动原 WorkTurn，并在意图识别时从动作文本中移除已匹配角色名；提交 `46b22f2` 的完整 CI 已通过。后续本地整合验证覆盖类型检查、生产构建预算、SQLite 迁移回放以及聊天/群聊/委派/任务/日程定向 90 项。第四批仍保留授权一致与任务/日程审批接线的最终复核，完成前不标记整批结束。

提交 `8ded612801490c29935181cfe22cd6cf12c87656` 的 CI `34005578820` 已完整成功，包含 1831 项单元/集成、schema 46 迁移回放和核心浏览器冒烟。

第四批最终审批接线已完成本地实现：Task Center 在已 claim 的 TaskRun/WorkTurn 上按实际步骤承担者准备至多一个 Skill 动作，Task/Run/Turn 同步进入等待；批准、拒绝或过期后复用原计划、SkillAction 和 WorkTurn，不重新 prepare。日程统一通过现有 Conversation Queue 与 `runQueuedDirect`，schema 47 持久化 `waiting-approval`；审批继续后即时收敛原 schedule run，重启保留待审批和未执行队列。新增真实受控浏览器 Adapter 回归，覆盖 Task Center 批准/拒绝、审批跨重启及日程批准；均断言审批前无 AgentRun/Adapter 副作用、审批后只执行一次。整仓生产构建/预算通过，329 个测试文件中 328 个、1833 项通过、1 项跳过；唯一失败为 `server.ts` 超过 600 行 7 行，压缩装配表达后架构与审批 6 项复验通过，交由精确提交 CI 完整复核。

第四批提交 `c3abe2fd38d32a494dd8a69adca6e1860063a470` 的 CI `34007597096` 已完整成功，含 1835 项单元/集成、schema 47 迁移回放和核心浏览器冒烟；第四批完成。

第五批首个纵切：自动日程到期扫描改为只原子受理并唤醒共享队列，不在 `runDue` 内等待模型，因此一个长任务不会阻塞后续独立日程的 claim。查询排除已有 running/waiting 日程，周期错过采用明确的 coalesce 并直接推进到未来时间片；时区字段先严格校验 IANA 标识并继续保存绝对 UTC instant，不宣称已经提供 DST 墙钟调度。TaskSchedule 与 ConversationQueue 的关闭和 Stop 增加默认 5 秒期限，超时持久化 `shutdown-timeout-unknown-result` 或 `stop-timeout-unknown-result`，晚到结果不能覆盖终态。当前仍需完成普通队列防饥饿公平性、accepted/started 时间事实及真实 dispatcher 的压力证据，故第五批保持开发中。

第五批首个提交 `b58a4e93a64d95c77680f87fb067b65a0314d9f1` 的 CI `34010019866` 已完整成功，含 1841 项单元/集成、迁移回放和核心浏览器冒烟。

第五批剩余实现完成本地验证：队列项等待 30 秒后进入持久时间驱动的 aging 集合，按 `enqueuedAt/id` FIFO 优先于持续新增的 `next`，同时保留短时插队、同 session 顺序锁和每角色两条 lane。schema 48 将日程 `acceptedAt` 与可空 `startedAt` 分离；自动受理不再冒充开始，首次 queue claim 与 WorkTurn 启动在同一事务写入 `startedAt`，轨迹分别记录“进入执行队列”和“开始执行”。旧 schema 47 时间安全回填。真实 ConversationQueue + TaskSchedule 压力回归证明一个悬挂执行不阻塞独立日程完成，并验证关闭/Stop 超时后的晚到 runner 无法覆盖 unknown-result。时间区语义明确为 IANA 标识校验加绝对 UTC instant；固定秒 interval 不冒充 DST 墙钟 cron。整仓 330 个测试文件、1845 项通过、1 项跳过，类型检查、生产构建与预算通过，交由精确提交 CI 复核。

第五批提交 `0e85ff4027b47ae6378d3ec07689fa717abf0a15` 的 CI `34011964404` 已完整成功，包含 1846 项单元/集成、schema 48 迁移回放和核心浏览器冒烟；第五批完成，进入第六批。

第六批首个纵切已完成本地复验：Harness adapter 将最终 runtime-facing 输入按 system、当前请求、实际重放历史、native schema/system/turn-context、live retained context 分项计量；固定 rc.1 wire schema 漂移门禁，活跃会话超限先验证 SQLite fresh replay，失败则保留旧 lane。Inspector 与 snapshot 读取同一无明文 `contextUsage` 投影，并保留最终重放序列和消息来源指针。Memory/Knowledge 检索在 FTS/LIKE 两条路径统一有界 overfetch、lexical score 与稳定 tie-break，修复 500/5000 候选被前 50 行截断、同数 stale mirror 和中文句尾词元丢失。当前定向 126 项通过，完整有效上下文还需继续核对 CharacterProfileRuntime/Inspector 与真实 provider 入参边界，故第六批保持开发中。

2026-09-07：继续整合本地未提交整改。SSE runtime/world 流统一使用有界连接缓冲，单个异常或慢订阅者只会被关闭，不影响健康订阅者；共享 world live 客户端在短暂卸载/重挂载窗口内复用连接。模型服务商目录改为优先读取仓库 `catalog/model-providers.json`，远程目录仅在仓库文件不可用且显式配置时作为备用，随后依次使用 stateRoot 缓存和内置快照。仓库目录仍经过同一严格解析器，目录变更在本地服务下一次读取时生效；批次 07/08 仍需其余审计范围和真实浏览器证据，未标记完成。

同日第六批补充：Harness adapter 在 direct embedder 未提供 `ContextBudget`、但 provider profile 声明上下文窗口时，预算回退现在按实际 `employeeSystemPrompt` 估算固定层，而不是只按裸 Persona 估算；新增回归验证展开后的身份/安全/工具提示进入预算且仍能在精确边界执行。适配器定向 32 项、类型检查和生产构建/预算通过；第六批仍保持开发中，尚未宣称覆盖真实供应商 tokenizer 或完整 provider canary。
