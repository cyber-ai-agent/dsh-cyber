# 2026-09-05 审计整改执行计划

审计来源：`dsh-cyber-audit-2026-09-05.md` v1.0。审计与开发起点均为 `3e922bfee3915c1072223654e52c026766ef2c0c`。执行分支：`codex/audit-remediation-20260905`。

## 执行方式

先验证发现仍成立，再以故障注入测试固定行为，最后实现修复。E1 独立语义复现必须转为仓库回归测试；E2 核对完整调用链；E3 保留为待验证风险，不能写成线上事故。

按以下顺序形成独立提交及审查单元。当前批次验证通过后继续下一批；公共契约变更由主 agent 统一整合，Luna max 子 agent 负责不冲突的实现、测试或审阅。每批记录测试结果、限制、提交号；未通过门禁不得标记完成。开发过程只使用隔离临时数据进行故障测试。

如拆分后续 PR，每个新开发分支重新 fetch 并从当时 `origin/main` 创建；依赖尚未合并时先处理依赖，不从旧开发分支宣称最新基线。本轮一个整改分支内按顺序提交，不重复创建相互依赖的平行实现。

## 九批交付

| 批次 | 范围 | 实现边界 | 必须提供的证据 | 状态 |
| --- | --- | --- | --- | --- |
| 01 | F01–F03 | 重试循环、运行映射、会话级 reset、适配器代际租约和退役 | 重试中审批/停止、双会话隔离、并发配置变更、工厂失败、关闭竞争 | 已提交，CI 通过 |
| 02 | F04/F05 | 稳定读取、临时发布及读回、恢复 journal、失败回滚与独占保护 | 同尺寸改写、增长/删除、逐步 rename 故障、进程中断后恢复、救援副本保留 | 已提交，CI 通过 |
| 03 | F07/F13/F17 部分 | 按世界和会话持有草稿，上传回调归属、逐文件状态；先采用诚实的“清空草稿”语义 | 上传中切会话/世界、部分失败、删除/卸载、可见入口浏览器冒烟 | 代码及本地验证完成，待 CI |
| 04 | F08 | 在现有 WorkTurn/队列上统一受理：聊天 → 任务 → 日程 | 同 key 幂等/冲突、授权一致、审批重启、无双重派发 | 待执行 |
| 05 | F09 | 依赖 04；日程受理解耦执行、有界并发、公平性、关闭期限 | 长任务不阻塞独立任务、停止期限、错过触发和时区、未知结果不重做 | 待执行 |
| 06 | F06/F10/F11 | 最终有效上下文、硬预算、检查器来源、稳定 FTS 候选 | 捕获实际入参比对、超限不请求、私聊边界、50/500/5000 候选评测 | 待执行 |
| 07 | F12/F14/F16 | SSE 缓冲上限、订阅异常隔离、状态所有权、保存反馈、布局退出/键盘 | 慢连接、异常订阅者、保存失败、pointercancel、Profiler 和滚动锚点 | 待执行 |
| 08 | F15 | 服务端路由解释与能力事实，未知能力显式呈现 | 继承矩阵、临时覆盖、删除回退、参数拒绝、缓存事实区分 | 待执行 |
| 09 | F17 与产品闭环 | 首次委托、持续合作、周期工作；交付验收、真实状态投影 | 必过核心浏览器冒烟；三视口、小屏、200% 缩放、键盘；3D/语音资源生命周期 | 待执行 |

F06 的硬固定层超限属于高优先级：在 01–03 后重新评估可达性，若测试确认会损坏正常请求，可提前交付独立拒绝保护；完整上下文整合仍属于 06，避免与统一受理层同时改装配。

只读复核已确认普通聊天最多可输入 32,000 个 Unicode 字符，而小窗口模型的固定文本统计会被压低；规划后仍会追加 Persona/世界/Skill/产物提示。安排在 03 后先交付 F06 固定输入拒绝保护（不静默丢弃当前请求）。该保护只约束明确计入的文本，不能冒充已经覆盖 Harness 保留历史与全部原生工具 schema；完整有效上下文与检查器一致性仍在 06 验收。

审计中的架构/交互建议不是自动生效的产品指令。第 14 节建议的左侧角色导航与仓库护栏冲突，因此保留左侧“仅会话”、顶栏统一市场/创意工坊、角色在档案管理的现有规则；不借整改恢复旧入口。性能数字仅作为待测目标：记录机器、数据量和方法后评估受理 p95 300ms、管理 API 200ms、输入回显 50ms、缓存会话切换 200ms，不提前宣称达标。

## 32 个功能面的验收登记

审计第 10 节的 32 项作为回归清单，而非 32 个新增功能需求。执行到所属批次时补充真实运行证据，尚未复测不宣称通过。

- 世界、角色与模型：启动配置；世界创建设置；归档恢复删除；角色版本授权；履历记忆成长；头像 VRM；模型继承。
- 对话与执行：私聊会话；排队补充插队；停止重试恢复；群聊；任务分工；审批；轨迹与上下文。
- 知识与交付：角色记忆检索；知识导入检索；图谱整理；任务中心；日程；产物中心。
- 技能与创作（21–28）：原生工具/宿主技能；MCP/Browser/Firecrawl；通讯 IM；市场安装升级回滚；角色生成器；世界生成器；皮肤生成器；插件生成器。IM 完整收发只作独立能力评估，本轮不扩大平台接入。
- 空间与系统（29–32）：2D/3D 空间；语音输入朗读；备份恢复更新诊断；多语言响应式可访问性。保留接线审阅/完整验证的差别，不以构建成功代替功能验收。

## 验证与交付标准

1. 每批运行受影响测试、类型检查与构建；跨契约修改追加相关服务集成测试。
2. 界面修改使用真实可见控件，记录 1440×900、1920×1080、3840×2160 的图片适配、空白、对比度、中文一致性、最小字号、可读性以及 console error/warn。
3. 真实 Harness canary 与无密钥确定性测试分别报告；没有真实执行证据不宣称供应商链路已通过。
4. 提交前检查差异范围及运行健康；不提交凭据、数据库、日志、生成产物。提交后 push，按精确提交检查 CI；合并与发布只采用实际通过的证据。
5. 备份/恢复测试不操作用户 stateRoot；更新不能删除本地世界、角色、资产或历史。

## 执行记录

- 2026-09-05：完成审计重点与九批依赖核对。fetch 后 HEAD、origin/main、merge-base 一致；工作树干净。开始 01，确认现有重试直接返回 Promise、角色级恢复和代际 await 竞争均存在。
- 批次 01：类型检查、生产构建及构建预算通过；全仓库 320 个测试文件通过，1760 项通过、1 项跳过（该轮之后补充的路由竞争测试单独 9/9 通过）。真实 Harness Worker 使用回环模型服务的权限、审批、会话恢复集成测试通过；未宣称外部供应商 canary 通过。隔离临时 stateRoot 的 `/api/health` 返回 ok，SQLite integrity 为 ok。无界面改动，不标记视觉验收。
- 批次 01 提交 `c10b32f` 已推送；草稿 PR #178；GitHub CI 运行 `33974316017` 的 required 成功，含干净依赖安装、类型检查、单元/集成与迁移/schema 门禁。继续 02，不提前合并未完成的整改草稿。
- 批次 02：备份/CLI/服务/独占锁定向回归通过，含 9 MiB 分片、强制恢复、进程终止后重新持锁、在线恢复拒绝、旧 schema、损坏包、回退失败及已提交事务清理；类型与构建预算通过。全仓库运行 1774 通过、1 跳过，另有装配模块类型导入循环与 Windows 临时目录 ENOTEMPTY 两项失败；循环已修正，两文件随后 13/13 定向复验通过，目录清理问题未修改产品代码，不把这次本地全量运行记为全绿，继续以远端完整 CI 复核。
- 恢复机制的验证边界为进程中断与明确 I/O 失败。临时文件和 journal 已刷盘，但未验证突然断电/磁盘硬件损坏下的所有文件系统保证。正式备份发布使用同文件系统硬链接，文件系统不支持时明确失败，不回退到覆盖旧备份。
- 批次 02 提交 `e2a1594e8574440cad8da9488f1bb88936701e90` 的远端 CI `33976037819` 已完整成功，`CI / required` 通过。
- 批次 03 的旧产物浏览器回归已失败复现：A 发起上传后切到 B，`owner-a.txt` 错误出现在 B 的草稿中。采用仓库 Playwright：当前没有 Browser plugin/browser skill（Browser plugin not available），不依赖外部模型；沿用现有深色/琥珀色设计 token 和现行产品信息架构。
- 2026-09-06 批次 03：104 个前端测试文件、541 项通过；随后 owner alias 修正的 3 文件 25 项定向复验通过。最终生产构建/预算通过，浏览器 Smoke 3/3 通过（13.6 秒），包含两个会话同时上传、世界切换、刷新、部分失败、清空及旧命令兼容。`/换个话题` 不再承诺重置上下文。浏览器 Smoke 已加入 required CI，失败保存截图/trace/console。

### 批次 03 视觉检查

| 视口 | 图片适配/空白 | 草稿文字对比度 | 语言/最小字号 | 文案与操作 |
| --- | --- | --- | --- | --- |
| 1440×900 | 背景与画布容器铺满，无新增黑边或横向溢出 | 附件文字 ≥ 4.5:1 | 中文状态，文件名原样；附件 ≥ 12px | 草稿可读；移除目标 40×40px |
| 1920×1080 | 容器铺满，无横向溢出 | 附件文字 ≥ 4.5:1 | 附件 ≥ 12px | 草稿可读；移除目标 40×40px |
| 3840×2160 | 容器铺满，按原比例呈現 | 附件文字 ≥ 4.5:1 | 附件 ≥ 12px | 草稿可读；移除目标 40×40px |

正常路径 console error/warn 为空；部分失败用例主动注入 HTTP 500，允许对应资源错误，pageerror 为空。截图已实际查看。世界默认 cover 取景仍可能让角色离开可见区域，本批不宣称整个世界/3D/语音及小屏视觉验收完成，完整取景和产品黄金路径仍在 09 处理。
