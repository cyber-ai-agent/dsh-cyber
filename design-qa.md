# DSH Cyber 视觉验收

## 既有世界视图验收

### 对照依据

- 用户参考：原赛博公司地图中央为一整块缺少层次的黑色地面。
- 当前实现：中央改为嵌入地面的协作中庭，用同材质像素风电路、低亮度节点和三枚状态标记补足空间叙事；不放置钟表、品牌标志、文案或阻挡角色移动的高物件。
- 对照方式：用户参考截图与 `artifacts/ui-world-conversations/world-1920x1080.png` 已在同一次视觉检查中并排审阅。

### 审批结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 中央空地 | 通过 | 原纯黑空地已改为可通行的嵌入式协作中庭，视觉焦点明确但不遮挡员工 |
| 世界入口与层级 | 通过 | 工作区与当前世界相邻；不再出现“世界 / 工作台”重复模式标签 |
| 多世界切换 | 通过 | 赛博公司与月影酒馆均可从顶部下拉切换，角色、会话和地图同步变化 |
| 地图自适应 | 通过 | 1440×900、1920×1080、3840×2160 均铺满右侧容器，无错误留白或小块居中 |
| 侧栏比例稳定 | 通过 | 世界、档案、文件、预览共享同一可拖动宽度，切换标签不改变比例 |
| 文字清晰度 | 通过 | 正文至少 14px，辅助信息至少 12px；未使用父级透明度压低文本对比度 |
| 语言一致性 | 通过 | 用户界面文案中文优先；英文仅保留品牌、发布者、版本和技术标识 |
| 安装状态 | 通过 | 已安装插件显示“已安装”，已招募员工显示“已招募”，不再显示重复安装操作 |
| 模型配置 | 通过 | 自定义本机、局域网或 HTTPS 模型可新增、编辑、重启恢复和删除；界面不再提供温度项，凭据只保存环境变量名 |
| 控制台 | 通过 | 本地浏览器检查无未解释的 error / warn |

## 数字人行动舱验收

### 验收对象

- 视觉基准：用户选定的“全息行动舱”方案。
- 实现状态：三角色群聊，主角色处于真实工具执行中，两名协作者已加入会话。
- 核心约束：行动舱只把真实运行状态用于人物和状态面板；耐久执行事实统一进入“轨迹”，不在画面顶部重复堆一条流程导航。

### 对照修正

- P0：无。
- P1：4K 高容器下人物沿用普通屏幕尺寸，造成舱体上方空白过大。已改为尺寸容器查询，在高容器中重新放大并居中人物、协作者和身份条。
- P2：首版主角色相对参考图偏小。已扩大主体比例，同时保留右侧状态脊柱和两侧协作者的可读空间。
- P1：角色图集存在跨单元格残片。已替换为逐角色连通域清理后的透明 4×4 说话帧图集，移除旧图集。
- 设计差异：参考图顶部流程条已按用户反馈移除；事实仍在“轨迹”中可追溯。

### 动效与交互

- 待命：低频呼吸，避免持续大幅位移。
- 思考：人物光晕与扫描线。
- 执行：能量明暗脉冲，状态脊柱同步高亮“执行中”。
- 待审批：悬停式警示光晕。
- 失败：停止运动、降低饱和度并切换危险色。
- 说话：支持关闭、手动与自动播报，声音从系统语音列表选择并按世界记忆；闭嘴/开口真实帧随播报交替，回复文本不上传，代码块与 URL 不朗读。
- 状态：原“状态脊柱”改为贴右的“状态”，支持收起并按世界记忆。
- 用户可切换“静态模式”；系统开启 `prefers-reduced-motion` 时自动禁用动画。
- 数字人/地图收敛为一个模式切换按钮；静态动效移动到语音设置内。点击主角色打开档案，点击协作者返回对应会话。

### 多视口验收

| 视口 | 图片适配 | 大段空白 | 文字对比度 | 中文一致性 | 最小字号 | 横向溢出 |
| --- | --- | --- | --- | --- | --- | --- |
| 1440×900 | 通过 | 通过 | 通过 | 通过 | 12px 辅助 / 14px 正文 | 无 |
| 1920×1080 | 通过 | 通过 | 通过 | 通过 | 12px 辅助 / 14px 正文 | 无 |
| 3840×2160 | 通过（高容器重排） | 通过 | 通过 | 通过 | 12px 辅助 / 14px 正文 | 无 |

截图保存在 `artifacts/digital-human-world/`，浏览器控制台记录为 `console.log`，结果为无 error/warn。自动化验证了真实执行事件、状态折叠、说话帧切换、手动/自动语音、多声音选择、系统减弱动态效果、静态模式、地图切换及刷新后的偏好恢复。

## 当前自动化结果

- `pnpm typecheck`：通过。
- `pnpm test`：179 个测试文件通过，778 项通过、1 项跳过。
- `pnpm test:smoke`：13 个浏览器场景全部通过。
- 构建预算：通过。

## 最终结果

final result: passed
